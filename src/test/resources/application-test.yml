spring:
  datasource:
    url: jdbc:h2:mem:question_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MYSQL
    username: sa
    password: sa
    hikari:
      auto-commit: false
  jpa:
    generate-ddl: true
    show-sql: true
